<%= form_with(model: company_verification) do |form| %>
  <% if company_verification.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(company_verification.errors.count, "error") %> prohibited this company_verification from being saved:</h2>

      <ul>
        <% company_verification.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :company_email, style: "display: block" %>
    <%= form.text_field :company_email, required: true %>
  </div>

  <div>
    <%= form.label :company_name, style: "display: block" %>
    <%= form.text_field :company_name, required: true %>
  </div>

  <div>
    <%= form.label :verification_token, style: "display: block" %>
    <%= form.text_field :verification_token, required: true  %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
